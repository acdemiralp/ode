#pragma once

#include <cstddef>

namespace ode
{
template <typename tableau_type>
constexpr std::size_t order          = 0;
template <typename tableau_type>
constexpr std::size_t extended_order = 0;
}